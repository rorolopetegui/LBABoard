/*
 * Controllers Messages
 *
 * This contains all the text for the Controllers container.
 */
import { defineMessages } from 'react-intl'

export const scope = 'app.containers.Controllers'

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the Controllers container!',
  },
});
